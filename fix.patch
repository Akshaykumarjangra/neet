--- a/client/src/pages/AdminMarketing.tsx
+++ b/client/src/pages/AdminMarketing.tsx
@@ -149,7 +149,7 @@
     });
   };

-  if (!user?.isAdmin && !user?.isSuperAdmin) {
+  if (!user?.isAdmin && user?.role !== 'admin') {
     return (
       <div className="min-h-screen bg-background">
         <Header />
--- a/client/src/pages/AdminDashboard.tsx
+++ b/client/src/pages/AdminDashboard.tsx
@@ -133,7 +133,7 @@
   });

   useEffect(() => {
-    if (user && !user.isAdminOrMentor) {
+    if (user && !(user.isAdmin || user.role === 'mentor')) {
       setLocation("/");
     }
   }, [user, setLocation]);
@@ -159,7 +159,7 @@
   };

   // ALL hooks MUST be above early returns (React rules of hooks)
-  const isAuthorized = !!user?.isAdminOrMentor;
+  const isAuthorized = !!(user?.isAdmin || user?.role === 'mentor');

   const { data: users = [], isLoading } = useQuery<User[]>({
     queryKey: ["/api/admin/users"],
--- a/client/src/pages/AdminContentManager.tsx
+++ b/client/src/pages/AdminContentManager.tsx
@@ -160,7 +160,7 @@
     setLocation(`/admin/content?tab=${value}`, { replace: true });
   };

-  if (!user || !user.isAdminOrMentor) {
+  if (!user || !(user.isAdmin || user.role === 'mentor')) {
     setLocation("/");
     return null;
   }
--- a/client/src/data/videos.ts
+++ b/client/src/data/videos.ts
@@ -73,7 +73,6 @@
         title: "Work, Energy, and Power",
         description: "Definitions of work, kinetic energy, potential energy, and power.",
         subject: "Physics",
-        type: "video",
         youtubeId: "w4QFJb9a8vo", // CrashCourse Physics #9
         duration: "9:55",
         channel: "CrashCourse",
@@ -831,7 +830,6 @@
         description: "Tangent lines, rates of change, and optimization.",
         subject: "Math",
         youtubeId: "4_w_p6q2O_8", // Khan Academy (Placeholder - reusing straight lines ID as placeholder for app of derivatives if specific one unavailable, using organic chem tutor for real one)
-        type: "video",
         duration: "35:20",
         channel: "The Organic Chemistry Tutor",
         topics: ["Calculus", "Optimization", "Rates"],
--- a/client/src/components/simulations/DnaHelixViewer.tsx
+++ b/client/src/components/simulations/DnaHelixViewer.tsx
@@ -44,7 +44,7 @@
     const controls = new OrbitControls(camera, renderer.domElement);
     controls.enableDamping = true;
     controls.dampingFactor = 0.05;
-    controls.autoRotate = false; // We handle rotation manually
+    (controls as any).autoRotate = false; // We handle rotation manually

     // Lighting
     const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
--- a/client/src/components/DashboardMetrics.tsx
+++ b/client/src/components/DashboardMetrics.tsx
@@ -2,6 +2,7 @@
 import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
 import { Progress } from "@/components/ui/progress";
 import { BookOpen, Target, Clock, TrendingUp } from "lucide-react";
+import { Button } from "@/components/ui/button";

 interface DashboardMetricsProps {
   totalQuestions: number;
--- a/client/src/components/ChapterQuiz.tsx
+++ b/client/src/components/ChapterQuiz.tsx
@@ -175,7 +175,7 @@
                     <>
                         <QuestionCard
                             questionNumber={currentQuestionIndex + 1}
-                            difficulty={getDifficultyLabel(currentQuestion.difficultyLevel) as "Easy" | "Medium" | "Hard"}
+                            difficulty={getDifficultyLabel(currentQuestion.difficultyLevel ?? undefined) as "Easy" | "Medium" | "Hard"}
                             subject={subject || ""}
                             topic={chapterTitle || ""}
                             question={currentQuestion.questionText}
