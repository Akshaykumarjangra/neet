# Local development database (Postgres running via docker-compose)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/neetprep
DATABASE_SSL=false

# Adjust as needed
PORT=5001
NODE_ENV=development
